# Job Apply Pro

Job Apply Pro is a local-first Windows desktop application for coordinating job discovery, qualification, application workflows, and durable tracking. The product keeps deterministic state, security, validation, and browser control around bounded AI-assisted tasks.

## Reviewed Browser Upload Reconciliation build

The active source milestone is **Reviewed Browser Upload Reconciliation `v0.77.0-alpha.1`**. Before one exact reviewed Greenhouse document upload crosses Playwright, the backend prepares encrypted reconciliation intent bound to its durable operation and only attempt, exact request/control/locator, source origin/page/stage/form review/upload status, and the staged file's name, byte count and SHA-256. The saved action redacts its path and staged plaintext is cleared after the one attempt. If the worker response is lost, Operations offers **Verify current upload outcome** only when fresh read-only observation shows exactly that filename once on the same origin, page type and recognized Greenhouse stage with a changed fingerprint. Preview and approval each reprove the result. Approval records only local filename evidence; it never uploads again and does not prove provider receipt, bytes, retention, parsing, attachment or submission. The original operation and attempt remain `UNCERTAIN`. See [ADR-0087](docs/adr/0087-reviewed-browser-upload-reconciliation.md).

Exact source checkpoint `22fe11e7276089a46b65f3ec0a41d566a45ebfc5` passes **1,926 backend tests** at **86.59% coverage** across **17,853 statements** and **521 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **242 files**; strict mypy passes **210 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract build/test declaration and the production Electron build pass. Both dependency audits report no known vulnerabilities; pip-audit skips only the unpublished editable local backend package. The tracked tree contains zero private-key and common-token marker files. Candidate checkpoint `909c3fe6d579881bedc9ffb2c5e60c22c5840646` passes both packaged-backend smokes and the **2/2** installer-delivered supervisor/recovery protocols. The byte-identical backend and worker copies are each 18,897,518 bytes, SHA-256 `BB39A0324634B5DD43751EE3B55A5E53162B6457637011ACD8DCE053A37A21FB`. The unpacked desktop is 225,500,672 bytes, SHA-256 `ED0F1B7D6B81F4BAAA819D5F0BE8621E892C620C1C918D9F983DABE1ECE1F681`; `app.asar` is 33,662,435 bytes, SHA-256 `5121E7BC9049F5A4E83515A5546BDCF2226C0AE72CA8F48A81ECAFEC0D4259B0`; the installer is 189,246,968 bytes, SHA-256 `203C574C22BC8A0110E48966B41D99652D34C32F2307E8399191416CEC9CA6EF`; its 180,065-byte blockmap has SHA-256 `B73FC6BC0469B764947F5F9585FFA72E67041C2F2A251C627A46A2E9F0E6F2E8`; and `latest.yml` is 378 bytes, SHA-256 `0F4C1C38874C90DECF65C77C5F5AC01F099FE51D98B8C87D8A13EE5B18675A8B`. Every executable reports `NotSigned`. Greenhouse remains disabled for production and has no authorized live-portal compatibility evidence. Protected integration, trusted signing, physical installed-app testing, signed update/rollback and live-provider acceptance remain separate external gates; no v0.76 result transfers.

## Reviewed Browser Navigation Reconciliation build

The active source milestone is **Reviewed Browser Navigation Reconciliation `v0.76.0-alpha.1`**. Before one exact reviewed Greenhouse stage-navigation click crosses the Playwright boundary, the backend can now prepare an encrypted reconciliation intent bound to its durable operation, only attempt, source origin, page/stage, form-review fingerprint, control, locator, and request fingerprint. If the worker response is lost, Operations may offer **Verify current form stage** only when a fresh read-only observation proves the same origin and a different, recognized, strictly later non-confirmation Greenhouse stage. Preview and approval each reprove the outcome. Approval records only reconciliation evidence; it never replays the click, uploads, or submits, and the original operation and attempt remain `UNCERTAIN`. See [ADR-0086](docs/adr/0086-reviewed-browser-navigation-reconciliation.md).

Exact source checkpoint `22435b2847ad481e5423c0a97767e85f9cc4f410` passes **1,909 backend tests** at **86.64% coverage** across **17,636 statements** and **516 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **242 files**; strict mypy passes **210 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract build/test commands, and the production Electron build pass under Node 24.19.0, pnpm 11.19.0, and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits report no known vulnerabilities; pip-audit skips only the unpublished editable local backend package. The tracked-tree scan finds no private-key marker. Candidate checkpoint `00effd641a928fe33f69d8b70baae939d2319214` passes both backend package smokes plus the opt-in delivered supervisor and recovery-controller protocols. The byte-identical backend and worker copies are each 18,889,142 bytes, SHA-256 `ACB136558BC2EB1293F389CB1C70582E4C629276D5A4E86D217F4DA7EDAE7320`. The unpacked desktop is 225,500,672 bytes, SHA-256 `48EA08F3DF74C43A02567312CA3BC6D520227DD58CD961699836C1B72EE735F2`; the installer is 189,230,565 bytes, SHA-256 `0DF57B0242BC418DE489128A904CD66A67091F09B4CB6B90B6AAD87AB95D6353`; and its 179,933-byte blockmap has SHA-256 `61EA06069954B61FA303C19203F7BD2276A95CCD0F98FC6F3D658A3B14DAA96A`. All executable artifacts report `NotSigned`. Greenhouse remains disabled for production and has no authorized live-portal compatibility evidence. Protected integration, trusted signing, physical installed-app testing, signed update/rollback, and live-provider acceptance remain separate external gates; no v0.75 result transfers.

## Reviewed Profile Quarantine Recovery build

The active source milestone is **Reviewed Profile Quarantine Recovery `v0.75.0-alpha.1`**. A failed v0.75 profile retirement now leaves one profile-associated quarantine that can be rediscovered after restart as `CLEANUP_PENDING`. The backend issues a new fingerprint only for exact stopped history, a missing canonical profile, one safe isolated directory, and its current bounded inventory. Electron displays a cancel-default native warning and approves only the immutable backend preview. The renderer never supplies or receives a path, inventory, cookie, token, credential, or claimed deletion result. Durable session/effect/checkpoint evidence and historical-name blocking remain. Anonymous v0.74 quarantines, provider logout/revocation, forensic secure erasure, automatic retention, and bulk cleanup remain separate. See [ADR-0085](docs/adr/0085-reviewed-profile-quarantine-recovery.md).

Exact source checkpoint `18cc87691558bfe0509a5c0d48e1791b7ba99783` passes **1,903 backend tests** at **86.67% coverage** across **17,493 statements** and **512 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **242 files**; strict mypy passes **210 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract build/test commands, and the production Electron build pass under Node 24.19.0, pnpm 11.19.0, and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits report no known vulnerabilities; pip-audit skips only the unpublished editable local backend package. The tracked-tree scan finds no private-key marker. Candidate checkpoint `783c6662071b5a8001d3e27f6b491362aaabb19b` passes both backend package smokes plus the opt-in delivered supervisor and recovery-controller protocols. The byte-identical backend copies are 18,884,020 bytes, SHA-256 `0ED35F2356A9957FA0AAA58574B68E9F1138B382725E0AE138A38361697CFE93`; the byte-identical worker copies are 18,884,020 bytes, SHA-256 `F669D2DF9DB4EAF4B272714DAB6D3F52840AD000368610D159C60A077AA9B961`. The unpacked desktop is 225,500,672 bytes, SHA-256 `69E7162FDC67204D58E188E9C007BB0E02A3C2FCDA2C43067B1301FBCB440796`; the installer is 189,218,090 bytes, SHA-256 `5CA7229E08B86D7DAB81ED96FB8D246CAED94F72AA5AD739DAEB5E14F799887A`; and its 181,751-byte blockmap has SHA-256 `0AF7948F00CB978914D91225D5107A2CA565303842E042AE1F76AE59377BC856`. All executable artifacts report `NotSigned`. Protected, signed, authorized live-provider, and physical installed-app evidence remains pending; no v0.74 result transfers.

Reviewed Portal Profile Retirement `v0.74.0-alpha.1` remains the local profile-removal boundary. The backend inventories each historical browser profile as available, active, retired, or inconsistent and issues a fingerprinted retirement preview only for an exact, stopped, ordinary profile directory. Approval atomically isolates the backend-derived directory, rechecks its bounded inventory, removes only that quarantine, retains session evidence, and prevents the historical name from silently recreating storage. Lifecycle operations are serialized across request-scoped services. See [ADR-0084](docs/adr/0084-reviewed-portal-profile-retirement.md).

Exact source checkpoint `3aa119fb89f1ba241accea44b0d48183eb726916` passes **1,901 backend tests** at **86.69% coverage** across **17,370 statements** and **508 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **242 files**; strict mypy passes **210 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract build/test commands, and the production Electron build pass under Node 24.19.0, pnpm 11.19.0, and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits report no known vulnerabilities; pip-audit skips only the unpublished editable local backend package. The tracked-tree scan finds no private-key marker. Candidate checkpoint `3b1db2479cce4b928df9d49c4072da29bafa0c91` passes both backend package smokes plus the opt-in delivered supervisor and recovery-controller protocols. The byte-identical backend copies are 18,880,218 bytes, SHA-256 `DD03CD66EDBDCDEAB89803FCB0927372F71F9F4081F7258C3C969B836796E39B`; the byte-identical worker copies are 18,880,218 bytes, SHA-256 `BEC0018E9B987B34F1925213116961490C50955A6B36EE1B2978285FB144A86F`. The unpacked desktop is 225,500,672 bytes, SHA-256 `ECC2237570328F749B67DB96EFDA1943839C7F70CB09BD9D8937314EDFC5AC28`; the installer is 189,209,443 bytes, SHA-256 `63649F6B2928C50D7189990BE414F9D9EC068035A5D483EA9D98A04B66AE7424`; and its 181,560-byte blockmap has SHA-256 `0116153AD0D3F217F7FDD143CE2ED5E17FECE9C4378D093E5CB56CFCC1954DC1`. All executable artifacts report `NotSigned`. Protected, signed, authorized live-provider, and physical installed-app evidence remain pending; no earlier result transfers.

Origin-Bound Portal Profiles `v0.73.0-alpha.1` remains the profile-reuse boundary. Every persistent browser profile is bound across historical sessions to one engine, exact case-preserved name, and exact normalized origin set. Case aliases, active reuse, conflicting histories, and another origin set fail before worker entry. Users sign in directly in the visible browser; Job Apply Pro stores no portal password or security code and never claims that a saved profile is authenticated. See [ADR-0083](docs/adr/0083-origin-bound-portal-profiles.md).

Exact source checkpoint `f2bc3604b4a60df4905b16a58df18e281211c075` passes **1,896 backend tests** at **86.66% coverage** across **17,185 statements** and **503 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **241 files**; strict mypy passes **209 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract type/lint/build/test commands and the production Electron build pass under Node 24.19.0, pnpm 11.19.0 and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits report no known vulnerabilities; pip-audit skips only the unpublished editable local backend package. The tracked-tree scan finds zero common token patterns and private-key markers. Candidate checkpoint `584263c5e4bb9c9633d314e53fcb72a591daf3ee` passes both backend package smokes plus the opt-in delivered supervisor and recovery-controller protocols. The byte-identical backend copies are 18,872,636 bytes, SHA-256 `4DF37B969DCCD5633C987D072F149DA9F1AE34F13BFF2CF208FD0758DA272214`; the byte-identical worker copies are 18,872,636 bytes, SHA-256 `FABFCAA5CABEBEDD476CD249CA0D3B250472430EE27098A143090562D85E98C2`. The unpacked desktop is 225,500,672 bytes, SHA-256 `5E9C8E033EE0EAC59F4842391071F38B856EBA931C13BB44D178B8C4E42C8484`; the installer is 189,192,810 bytes, SHA-256 `0AB83EB6B70487CDDD011A95F8DB86E50BF5F7DDA9E1D1CBDEA5E35C6094355B`. All executable artifacts report `NotSigned`. Protected, signed, authorized live-provider and physical installed-app evidence remain pending; no earlier result transfers.

Interrupted Forward Restore Resume `v0.72.0-alpha.1` remains the active recovery boundary. A blocked v2 restore can be explicitly rolled back to its sealed before-images or resumed to its sealed after-images. Inspection authenticates the complete operation and exposes distinct action-bound fingerprints. The native recovery dialog defaults to **Keep workspace blocked**; once `ROLLBACK` or `RESUME` is published, the direction cannot change. Resume skips exact completed targets, installs only retained encrypted after-images, keeps the database last, and never rereads staging files, the backup archive, or restored SQLite data. Terminal success requires an authenticated receipt, complete target reinspection, and a cleared guard. See [ADR-0082](docs/adr/0082-interrupted-forward-restore-resume.md) and [ADR-0071](docs/adr/0071-durable-restore-rollback.md).

Exact source checkpoint `496d9c9b3d80579c47484923013ab48625267934` passes **1,895 backend tests** at **86.64% coverage** across **17,176 statements** and **502 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **241 files**; strict mypy passes **209 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract type/lint/build/test commands and the production Electron build pass under Node 24.19.0, pnpm 11.19.0 and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits report no known vulnerability; pip-audit skips only the unpublished editable local backend package. The source diff scan finds zero common secret assignments and private-key markers. Candidate checkpoint `42bdc02544e67220e38c1590cc8092833adcfec0` passes the backend-dist and final delivered-runtime smokes plus the opt-in supervisor and native recovery protocols. The unsigned installer is 189,191,007 bytes with SHA-256 `E7162435FE0DF24D765282BF80F83A852F8E154EE2EAD37BF7DC5DDB1233814F`; all executable artifacts report `NotSigned`. Protected, signed, authorized live-provider and physical installed-app evidence remain pending; no earlier result transfers.

Reviewed Browser Field Reconciliation `v0.71.0-alpha.1` remains the active browser-effect safety boundary. Before an eligible reviewed field write crosses the Playwright boundary, the runtime stores an encrypted intent tied to the exact durable effect operation, attempt, request fingerprint, source page fingerprint, action kind, and deterministic verification. If the worker response is lost and the effect becomes `UNCERTAIN`, Operations may offer **Verify current field outcome** only while the same visible page remains in user takeover. Preview and approval each perform a read-only postcondition check; approval records local evidence without repeating the mutation. See [ADR-0081](docs/adr/0081-reviewed-browser-field-reconciliation.md).

The guide suggests navigation through readiness, evidence, application fields, challenges, and follow-up; it does not advance state or grant permission. Backend `allowed_controls`, exact review fingerprints, provider configuration, durable effect admission, encrypted reconciliation intent, and service policies remain authoritative. Operations exposes no generic retry, clear, or operator-success mutation. Production portal submission remains disabled.

The v0.71 release gate covers encrypted pre-dispatch intent, exact operation/attempt ownership, worker-response loss, positive and negative postcondition proof, approval-time reproof, immutable uncertain history, resolved-attention projection, authenticated API routing, strict IPC identifiers, cancel-default native confirmation, renderer gating, migration/restore semantics, and a real Chromium read-only probe. Exact source checkpoint `74428d81e540bdf1cad97ebf7b8e85ae10df2975` passes **1,874 backend tests** at **86.63% coverage** across **17,126 statements** and **498 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **241 files**; strict mypy passes **209 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract type/lint/build/test commands and the production Electron build pass under Node 24.19.0, pnpm 11.19.0 and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. Both dependency audits pass; pip-audit skips only the unpublished editable local backend package. The changed-content scan finds zero known pasted account credentials, common secret assignments, and private-key markers. Schema `20260915_0030` protects 47 tables. Greenhouse remains `production_enabled=false`, its live-validated page-type list remains empty, and supervised/final-submit execution remain disabled by default. Exact v0.71 package/protocol, protected, signed, authorized live-provider, and physical installed-app evidence remain pending; no older result transfers.

The historical v0.70 sanitized and loopback tests cover bounded/unsafe widget classification, dedicated binding compatibility, provider-scoped required-field coverage, exact reviewed option execution, cross-portal/collapsed/unowned/multiselect refusal, disabled-option exclusion, renderer mapping, and live worker refusal after the widget changes to collapsed. Exact source checkpoint `c4db1aa232ca6e2d8bb2815faf33702b0c74aebb` passes **1,863 backend tests** at **86.82% coverage** across **16,850 statements** and **492 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **239 files**; strict mypy passes **208 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract type/lint/build/test commands and the production Electron build pass under Node 24.19.0, pnpm 11.19.0 and Python 3.12.14. The contracts test command currently has no runtime test files and exits successfully by declaration. The changed-content scan finds zero common secret assignments and zero private-key markers. Exact v0.70 package/protocol, protected, signed, authorized live-provider and physical installed-app evidence remain pending; no older result transfers.

The historical v0.69 sanitized tests cover missing/stale Greenhouse form review, multiple and unsafe submit controls, cross-portal review injection, unrecognized request authority, exact-locator dispatch, renderer composition, off-origin and identifier-free uncertainty, no retry after an ambiguous click, and identifier-backed confirmation. Exact source checkpoint `10c981d27f49a1acefa9401f6e020b67eba07afe` passes **1,846 backend tests** at **86.93% coverage** and **491 default desktop tests** with two explicit packaged-runtime opt-ins skipped. Ruff lint/format passes **239 files**; strict mypy passes **208 source/test/helper files**. Workspace Prettier, oxlint, TypeScript, shared-contract type/lint/build/test commands and the production Electron build pass; the contracts test command currently has no runtime test files and exits successfully by declaration. The unsigned v0.69 backend distribution, unpacked app, NSIS installer, delivered-backend smoke, supervisor lifecycle, and recovery-controller protocol also pass local qualification under Node 24.19.0 and Python 3.12.14. This evidence does not transfer to v0.70 or establish live compatibility. Protected CI/security, Authenticode signing, authorized live-provider/portal use, and physical installed-app acceptance remain separate.

The v0.63 durable external-effect ledger remains the active safety foundation. Browser actions and AI completion/embedding provider calls commit privacy-safe logical ownership and a `DISPATCHING` attempt before external I/O. Exact replays are consumed, changed claims conflict, and ambiguous responses, lost local evidence, or interrupted dispatches become `UNCERTAIN` without automatic retry or provider fallback. Schema `20260915_0030` extends the protected history to 47 tables with encrypted browser-field reconciliation intent; the specialized mail, calendar CREATE, and Gemini-cleanup journals remain authoritative for their effect families. See [ADR-0073](docs/adr/0073-durable-external-effect-ledger.md) and [ADR-0081](docs/adr/0081-reviewed-browser-field-reconciliation.md).

The completed v0.61 recovery foundation continues to seal authenticated before/after images for every restore target, protect the compiled history graph, use default-deny in-memory SQLite bookkeeping, and require explicit reviewed rollback or authenticated finalization in an isolated packaged recovery session. See [ADR-0071](docs/adr/0071-durable-restore-rollback.md). Its exact package/protocol gates, protected pull-request integration and post-merge main checks pass; physical installed-app validation remains separate.

Saved public Greenhouse jobs support exact requirement classification, locked-evidence findings, separate eligibility approval and immutable resume selection. Missing or ambiguous requirements cannot produce a perfect score. Changed source, claims or document bytes invalidate current readiness without rewriting history. No portal action occurs. See [ADR-0070](docs/adr/0070-reviewed-job-readiness.md).

Reviewed Gmail/Outlook replies retain verified source/account/connection binding and immutable content. Offline previews stay unsendable until a fresh connected review; no automatic replies or delivery claims are added. See [ADR-0069](docs/adr/0069-source-bound-replies.md).

Public Greenhouse discovery remains available: manually preview a board, review one exact posting and import it for a local candidate profile. Fixed-origin unauthenticated reads send no candidate data; snapshots and deduplication are atomic and immutable. Imported jobs remain not evaluated, without mock advancement controls or a submission. See [ADR-0068](docs/adr/0068-greenhouse-public-discovery.md) for contracts, limits and remaining real-portal work.

Gemini retains one invocation-local work deadline across uploads, processing waits/status reads, inference and response construction. Polls target only the original registered resource; cleanup has a separate allowance and terminal retention errors. See [ADR-0067](docs/adr/0067-gemini-processing-budget.md). This is not hard OS/gateway cancellation and does not enable an unconfigured provider.

Offline restore retains exclusive workspace ownership, encrypted intent and persistent startup admission before replacement. V2 records include every target's before/after image and durable terminal evidence, so an interrupted operation can explicitly converge to its exact original or reviewed post-restore bytes without guessing or merging history. The original key and recovery records remain required. Legacy v1 evidence is completion/manual-recovery only. Recovery is user-selected, not automatic; multi-file replacement is not atomic and physical power-loss durability remains unproven. See [ADR-0066](docs/adr/0066-durable-restore-admission.md), [ADR-0071](docs/adr/0071-durable-restore-rollback.md), and [ADR-0082](docs/adr/0082-interrupted-forward-restore-resume.md).

Reviewed Gmail/Outlook drafts retain account binding, exact encrypted PDF/DOCX manifests, rechecked bytes and one-attempt reservations. Provider acceptance is not delivery. See [ADR-0065](docs/adr/0065-verified-mail-attachments.md); exact-version validation belongs in the readiness audit.

Startup, migration, readiness, shutdown and offline restore retain explicit child ownership, generation checks and bounded observation. The desktop waits for verified exit before quitting or installing an update; an uncertain restore blocks automatic restart. See [ADR-0064](docs/adr/0064-bounded-backend-lifecycle.md). This does not make multi-file restore atomic or establish whole-process-tree containment. The packaged browser-worker repair and loopback validation remain documented in [ADR-0063](docs/adr/0063-packaged-browser-runtime.md), with full image normalization in [ADR-0062](docs/adr/0062-validated-media-inputs.md).

Durable cleanup intent, encrypted resource identifiers, same-credential upload admission, deletion-only recovery, current consent and terminal retention errors remain in force. Different keys cannot prove shared account ownership; unknown outcomes require independent review. The **Durable media recovery** panel exposes safe status without media or secret identifiers. Leases are not hard real-time deadlines, and deletion is attempted rather than guaranteed. The readiness audit records version-specific validation and unsigned artifact evidence; none establishes signed-release or live-provider acceptance.

Backup archives are encrypted before storage, authenticated with the local master key, and verified by archive and per-entry SHA-256 hashes. Database replacement is excluded from the live API: the app stages and fingerprints restore plans, then its privileged supervisor stops the backend before invoking the offline recovery command. Replacement is blocked when either snapshot has unresolved provider-media cleanup, when the candidate would lose/change any compiled recorded-history dependency or revive restored-only authority/session/cache state, or when schema, encrypted fields, relationships or immutable documents cannot be safely verified. Each v2 restore retains exact encrypted before/after images under its unique `restore-control/operations` directory instead of overwriting an older `.pre-restore` copy. Production release jobs fail closed without a Windows signing certificate.

This remains an alpha foundation. Named portal execution is disabled by default and must be enabled per portal for an owner-approved validation window. Sign-in, MFA, CAPTCHA, assessments, legal attestations, and signatures remain direct user actions; final submission has its own disabled-by-default gate and exact confirmation flow. Catalog entries remain `production_enabled=false`, and mail/calendar writes still require reviewed OAuth authorization. See `docs/requirements-traceability.md` for the current product-scope audit.

## Architecture

```text
apps/desktop              Electron main/preload + sandboxed React renderer
backend/src/job_apply_pro FastAPI API, workflow domain, services, and storage
backend/src/job_apply_pro/browser Isolated Playwright JSON-lines worker and client
backend/src/job_apply_pro/documents Bounded document extractors and strict claim proposals
backend/src/job_apply_pro/ai Provider adapters, registry, prompts, privacy, and routing
backend/src/job_apply_pro/portals Typed portal adapters and verification contracts
backend/src/job_apply_pro/challenges Detection, answer mapping, timing, and AI routing policy
backend/src/job_apply_pro/integrations Provider-independent mail/calendar boundaries and fixtures
packages/contracts        Versioned TypeScript contracts shared by desktop code
backend/migrations        Alembic-managed SQLite schema
docs/adr                  Architecture decision records
```

Electron is the presentation and privileged local process boundary. Its main process generates an ephemeral API token, stores the master key with Electron `safeStorage`, runs migrations, supervises FastAPI, and exposes a small validated IPC bridge. The renderer receives neither Node.js access, database access, API credentials, nor encryption keys. Workflow state and events are committed together before the UI receives an update.

## Local encryption key

The managed desktop creates the master key and protects it with the operating-system encryption service. For standalone backend development only, set `JAP_MASTER_KEY` to a base64-encoded 32-byte value:

```powershell
python -c "import base64,secrets; print(base64.urlsafe_b64encode(secrets.token_bytes(32)).decode())"
```

The plaintext key is never committed, logged, sent to the renderer, or returned by an API. The managed desktop persists only the operating-system-protected key blob. Backups contain ciphertext and require the same key to restore.

## Prerequisites

- Windows 10 or 11
- Node.js 24 LTS
- pnpm 11
- Python 3.12 through 3.14
- Git

## Setup

```powershell
corepack enable
pnpm install
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".\backend[dev]"
python -m playwright install chromium
python -m alembic -c backend\alembic.ini upgrade head
```

## Development

The desktop development command starts and supervises the backend automatically:

```powershell
pnpm desktop:dev
```

For backend-only development, provide `JAP_MASTER_KEY` and optionally `JAP_API_TOKEN`, then run:

```powershell
pnpm backend:dev
```

The API listens only on `127.0.0.1:8765` by default.

Browser profiles default to `var/browser`, while screenshots and Playwright traces default to `var/browser-artifacts`. Encrypted candidate originals default to `var/documents` and are limited to 10 MiB per import. These paths are ignored by Git. Configure them with `JAP_BROWSER_DATA_DIR`, `JAP_BROWSER_ARTIFACT_DIR`, `JAP_DOCUMENT_DATA_DIR`, and `JAP_DOCUMENT_MAX_BYTES`; set `JAP_BROWSER_HEADLESS=false` only for supervised local debugging.

The Candidate Knowledge API is under `/api/v1/knowledge`. Supported imports are DOC, DOCX, PDF, RTF, TXT, and Markdown. DOC support is disabled until `JAP_DOCUMENT_LEGACY_CONVERTER_PATH` names an exact absolute LibreOffice `soffice` executable. Scanned-PDF OCR is disabled until `JAP_DOCUMENT_OCR_ENABLED=true` and `JAP_DOCUMENT_OCR_TESSERACT_PATH` names an exact absolute Tesseract executable; PDFium rendering remains bounded by page, DPI, and pixel limits. These external helpers never receive arbitrary command strings or run through a shell. Tailored DOCX/PDF generation requires a selected application and uses only locked verified claims approved for applications; the user reads every output paragraph and requirement gap before a fingerprint-bound native confirmation. Generated bytes and extraction data are encrypted, while an optional native save dialog exports the chosen result through Electron main without exposing bytes to the renderer. Confirmed portal uploads retain the selected version, displayed filename, SHA-256 digest, role, and upload fingerprint.

The AI Gateway API is under `/api/v1/ai`. Provider secrets, model definitions, and routing policies are supplied as local JSON through `JAP_AI_CONFIG_JSON`; never commit that value. External OpenAI-compatible endpoints require HTTPS, local endpoints require loopback, and a native `GEMINI` provider requires the exact `https://generativelanguage.googleapis.com/v1beta` base URL plus a local API key. Gemini interactions are sent with `store=false`; remote image URLs remain unsupported. An empty configuration leaves the gateway safely `not_configured`.

Authenticated media cleanup status is available at `GET /api/v1/ai/media-cleanup`; it performs no provider recovery. `POST /api/v1/ai/media-cleanup/retry` runs a bounded deletion-only pass, and `POST /api/v1/ai/media-cleanup/{record_id}/resolve` accepts a timestamp-bound operator acknowledgement only for unknown-resource manual review. Known resources with unreadable ciphertext remain retained and cannot be manually acknowledged away.

The reference portal API is under `/api/v1/portals`. `POST /reference/runs` prepares a loopback fixture application through `READY_TO_SUBMIT`; `POST /runs/{id}/confirm` accepts only the exact persisted review fingerprint and records confirmation only after the adapter observes its approved confirmation signal. This release does not authorize real portal submission.

The challenge API is under `/api/v1/challenges`. Detection persists the browser fingerprint, resume state, extracted questions, and timer. Suggestions come only from encrypted profile contact fields or locked approved answer-library entries. Legal attestations, signatures, and CAPTCHAs always require direct user action. Assessment completion requires verified answers, the current review fingerprint, and the fixed `COMPLETE CHALLENGE` phrase.

Connected Gmail and Outlook accounts can be read through `POST /api/v1/communications/providers/{provider}/messages/sync`. The desktop exposes this as **Sync messages** only after reviewed OAuth authorization grants read access. Initial Gmail synchronization captures mailbox history state before its bounded message read; later reads request only added-message history. Outlook tracks created messages in the Inbox through a complete provider-issued delta link. Provider reads stop after ten pages or 1,000 items, reject repeated or untrusted continuation state, and recover once when the provider declares a cursor expired. Opaque state is account-bound and encrypted locally, advances only after successful imports, and is never returned to Electron. Outlook attachment handling reads bounded filename metadata only; it does not download attachment content. Imported messages are analyzed and encrypted locally, and replayed provider message IDs reuse the existing record.

The portal catalog is available at `/api/v1/portals/catalog`, with fingerprint identification at `/identify` and sanitized replay validation at `/replays/validate`. Greenhouse supervised snapshots additionally include a transient `greenhouse_form` assessment when the current page matches a recognized form stage. Its `ready_to_advance` flag is explanatory only: it does not click, upload, grant authority, or prove live compatibility. Catalog and form-corpus support do not authorize live login, application completion, or submission.

Supervised portal runs are under `/api/v1/portals/supervised`. They require `JAP_AUTOMATION_ENABLED=true`, `JAP_SUPERVISED_PORTAL_ENABLED=true`, and an exact portal in `JAP_SUPERVISED_PORTAL_ALLOWLIST`. The visible browser remains in user takeover for authentication and challenges. Automated final submission additionally requires `JAP_SUPERVISED_PORTAL_SUBMISSION_ENABLED=true`, a current review fingerprint, exactly one recognized submit control, native desktop confirmation, and identifier-backed confirmation evidence.

The Communication API is under `/api/v1/communications`. Analyses and reply drafts are encrypted at rest; message listing, draft creation, calendar planning, follow-ups, mutation audits, and daily summaries share authenticated contracts. Gmail, Outlook, Google Calendar, and Outlook Calendar adapters fail closed until local OAuth credentials and account authorization are configured. Sanitized fixture adapters are used for regression tests and never contact provider networks.

The notification center derives bounded action projections from existing workflows, challenge sessions, communications, follow-ups, backups, and desktop update state. In-app alerts remain available by default. Native delivery must be enabled in the workbench and is limited to five new alerts per 60-second refresh. Notification text is intentionally generic because Windows may show or retain it outside the application. Selecting an alert opens only its fixed local workbench destination.

Provider registration control is available under `/api/v1/communications/configuration`. The desktop can validate and import a JSON file up to 64 KiB after a native confirmation. Accepted fields are public desktop client IDs, exact loopback redirect URIs, reviewed scopes, non-secret credential references/account hints, and policy flags. The active local configuration is AES-256-GCM ciphertext in the database. `JAP_COMMUNICATION_CONFIG_JSON` remains available for managed deployments and takes precedence; when present, desktop import and clear are disabled. Start from [`docs/examples/provider-configuration.example.json`](docs/examples/provider-configuration.example.json), replace only the public client IDs, and remove providers/scopes you are not using.

## Validation

```powershell
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
pnpm build
```

See [CONTRIBUTING.md](CONTRIBUTING.md), [SECURITY.md](SECURITY.md), [the production-readiness audit](docs/production-readiness-audit.md), [the failure-injection matrix](docs/failure-injection-matrix.md), and [docs/adr](docs/adr) before extending the architecture.
